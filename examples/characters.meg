fn main() -> i64 {
	let ascii: char = 'A';
	let utf8: utf8_char = u8'é';
	let unicode: uchar = u'😀';
	let escaped: char = '\n';

	if ascii != 'A' {
		return 1;
	}
	if utf8 != u8'é' {
		return 2;
	}
	if unicode != u'😀' {
		return 3;
	}
	if escaped != '\n' {
		return 4;
	}
	return 0;
}
